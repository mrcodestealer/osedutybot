# Optional — only needed when BOT_USE_AI=1 (natural-language commands).
# Install with the SAME Python that runs larkbot.service (fpmsbot env):
#   /root/miniconda3/envs/fpmsbot/bin/python -m pip install -r requirements-ai.txt
numpy
torch
safetensors
transformers>=4.36
